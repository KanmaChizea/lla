name: lla
description: A new Flutter project.
publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=3.0.5 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2
  arcore_flutter_plugin: ^0.1.0
  ar_flutter_plugin: ^0.7.3
  augmented_reality_plugin: ^4.0.1
  camera: ^0.10.5+2

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true

  assets:
    - lib/assets/model_56a_-_southern_white_rhino.glb
